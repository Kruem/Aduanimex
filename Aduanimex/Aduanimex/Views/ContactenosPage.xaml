<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Aduanimex.Views.ContactenosPage"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="CONTÁCTENOS"
             BackgroundColor="{StaticResource BacgroundColorPages}">
    <ContentPage.Content>
        <StackLayout BindingContext="{Binding Contacto}">
            <Image Source="ADXAzul.png"
                   WidthRequest="350"
                   VerticalOptions="Center"></Image>
            <Label Text="Cargando..." IsVisible="False" x:Name="labelLoading" />
            <WebView  x:Name="webView" WidthRequest="1000" HeightRequest="1000" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>